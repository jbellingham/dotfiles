I use ripgrep instead of regular grep. grep has been aliased to rg.
When creating summary documents, create them in ~/notes/claudedocs/${currentYear}/${currentMonth} and name them like so: ${YYYYMMDD}-${descriptiveName}.md where ${currentMonth} is the name of the month, and ${descriptiveName} is a kebab case name that describes the content of the document.

When writing code, apply a test-driven development approach. Write tests first, run them to ensure they fail, then write the code to make the tests pass. This ensures that the code is correct and that the tests are comprehensive. Prefer functional programming over imperative (for Ruby code, use dry-monads library constructs), but strike a balance between conciseness and readability. When refactoring code, do so in small increments and regularly run tests to ensure that they still pass. This helps to catch any regressions that may have been introduced. Pause at logical stopping points to allow me to commit changes.

When writing documentation, use clear and concise language. Use bullet points and subheadings to organize the information. Use examples and screenshots to illustrate the concepts. When writing code, use meaningful variable and function names. Use comments to explain the code. When writing documentation, use a consistent style and format. Use a table of contents to make it easy to find the information. Use a search function to find specific information.
